---
name: translate-webpage
description: "Translate webpage content (URL or local HTML) to clean Chinese Markdown for Obsidian. Optimized for Wiki articles and interviews — preserves image links and citation sources."
metadata:
  version: "2.3.0"
  model: "sonnet"
---

# /translate-webpage [url...|path...|@file] [--bilingual] [--output <dir>]

将网页内容（URL 或本地 HTML 文件）转化为 Obsidian 可用的干净 Markdown 文档，并逐句翻译为中文。专为 Wiki 百科和访谈文档优化——完整保留图片链接和引用来源链接。

支持同时输入多个 URL，每个 URL 由独立的子 agent 并行处理，大幅缩短批量翻译的总时间。

**架构：主 agent 负责编排（解析输入、启动子 agent、写入结果），翻译工作由子 agent 并行完成。子 agent 默认使用 `sonnet` 模型以获得更快的速度和更低的成本。**

## 参数

| 参数 | 必需 | 说明 |
|------|------|------|
| `[url\|path]...` | ✅ 必需 | 一个或多个网页 URL 或本地 `.htm` / `.html` 文件路径，空格分隔 |
| `@file` | — | 从文件中读取 URL 列表（每行一个 URL，支持 `#` 注释和空行）。可通过 IDE 选区传入 |
| `--bilingual` | 可选 | 启用双语对照模式（默认纯中文输出）。多 URL 模式下对所有 URL 生效 |
| `--output <dir>` | 可选 | 指定输出子目录（相对于 Obsidian vault 根目录），默认 `translate-webpage/` |

## 路由：单 URL vs 多 URL

翻译开始前，先判断输入模式：

```
输入中有多个空格分隔的 URL/路径？
  ├── 否 → 单 URL 模式（执行流程第1–6步，和之前一样）
  └── 是 / 输入为 @file → 多 URL 模式（跳至下方「多 URL 并行模式」）
```

**特殊标志：** `@` 前缀表示文件路径中含 URL 列表。例如 `@urls.txt` 或 `@C:\path\to\list.md`。该文件每行一个 URL，支持 `#` 注释和空行。当用户在 IDE 中选中包含 URL 列表的文档后调用此命令，选中的文本内容会作为伪文件传入——此时同样按每行一个 URL 解析。

---

## 单 URL 模式：执行流程

### 第一步：获取网页内容

#### A. 输入为 URL 时

1. **优先使用 `defuddle` skill** 提取干净的 Markdown（自动去除导航、侧栏、广告、页脚）
2. 若 defuddle 失败（如页面需要认证、重定向循环），回退使用 `WebFetch` 工具
3. 记录原始 URL，后续写入 frontmatter 的 `source` 字段

#### B. 输入为本地 HTML 文件时

1. 使用 `Read` 工具读取文件内容
2. 将 HTML 转换为干净的 Markdown：
   - 优先提取 `<article>`、`<main>`、`#content`、`.post-body` 等主要内容容器；若无则使用 `<body>`
   - **彻底去除**：`<nav>`、`<footer>`、`<script>`、`<style>`、`<aside>`、`<header>`、评论区、社交分享按钮、广告位
   - `<img>` → `![alt文本](src)` — 保留所有图片
   - `<a>` → `[文本](href)` — 保留所有链接
   - `<h1>`~`<h6>` → `#`~`######`
   - `<ul>`/`<ol>`/`<li>` → Markdown 列表
   - `<blockquote>` → `>` 引用块
   - `<table>`/`<tr>`/`<td>` → Markdown 表格（若表格过于复杂则保留 HTML）
   - `<br>` → 换行
   - `<hr>` → `---`
   - 原文中的 `&amp;` `&lt;` `&gt;` `&quot;` `&#39;` `&nbsp;` 等 HTML 实体解码

### 第二步：清洗与校验 Markdown

1. **去残留**：再次检查并移除遗漏的导航文本、侧栏链接、"相关文章"、"推荐阅读"、"分享到"等非内容元素
2. **图片链接审计**：列出所有图片 URL，标记数量供后续报告
3. **引用链接审计**：识别 Footnotes / References / 参考文献 / 出典 部分的链接，确保无一遗漏
4. **格式修正**：修复错误的 Markdown 语法（如缺少空行的列表、未闭合的代码块）
5. **引用区段重建（defuddle 拍平修复）**：defuddle 会将 Wikipedia 的多区段引用体系（Notes / References / External links）全部转换为统一的 `[^n]` 脚注列表，导致「注释」和「参考资料」标题下为空、所有条目堆在文末。必须在翻译前完成以下重建：

   **5a. 检测拍平信号**：若 Markdown 中同时满足以下条件，说明发生了拍平：
   - 存在 `## 注释` / `## Notes` 标题且下方无内容（仅有空行）
   - 存在 `## 参考资料` / `## References` 标题且下方无内容（仅有空行）
   - 文末存在密集的 `[^n]:` 脚注列表

   **5b. 分类脚注**：逐条检查每个 `[^n]`，按内容特征分为三类：

   | 类型 | 内容特征 | 目标区段 |
   |------|----------|----------|
   | **注释 (Notes)** | 解释性段落文字，包含多个完整句子，可能嵌入 `[^n]` 交叉引用。不含标准引文格式（作者-日期-标题-出版物） | `## 注释` |
   | **参考资料 (References)** | 标准引文格式：作者（日期）。「标题」。*出版物*。URL/存档/检索日期。或 ISBN、PDF 链接等结构化引用数据 | `## 参考资料` |
   | **外部链接 (External links)** | 以 `- [链接文本](url)` 列表项形式存在，不是 `[^n]:` 格式 | `## 外部链接` |

   **5c. 重建区段**：将分类后的条目移动到对应标题下方。参考资料部分可使用 `[^n]:` 脚注格式保留（正文中的 `[^n]` 引用仍能正确渲染），或转为编号列表。注释部分转为 `[^n]:` 格式并保留嵌入引用。

   **5d. 验证**：重建后，三个区段标题下均应有内容。不得存在空区段标题。

### 第三步：生成中文标题与文件名

1. 从页面 `<title>` 或 `<h1>` 提取原标题
2. 将标题翻译为中文作为文件名
3. 文件名去除非法字符：`\ / : * ? " < > |`
4. 文件名格式：`{中文标题}.md`

### 第四步：通过子 Agent 并行翻译

这是整个流程最关键的一步。**翻译工作由子 agent 完成**，主 agent 仅负责拆分内容和组装最终输出。

#### 4A. 内容分块策略

根据页面大小决定分块方式：

| 页面大小 | 策略 | 子 Agent 数量 |
|----------|------|---------------|
| < 2000 词 | 整篇发给 1 个子 agent | 1 |
| 2000–10000 词 | 按章节（`##` 标题）拆分 | 2–4 |
| > 10000 词 | 按章节拆分，每块不超过 3000 词 | 4–8 |

**分块原则：**
- 必须在段落边界处拆分，绝不能从段落中间切开
- 每块携带完整的 Markdown 格式（标题层级、图片、链接、脚注标记 `[^n]`）
- 参考资料/脚注部分作为独立的一块
- 双语模式（`--bilingual`）下，每块的 prompt 包含原文全文

#### 4B. 启动子 Agent

对每个分块，使用 `Agent` 工具启动一个子 agent，**必须指定 `model: "sonnet"`**：

```
Agent(
  description: "翻译第N块",
  prompt: <包含翻译铁律 + 语言规则 + 本块原文>,
  model: "sonnet"
)
```

子 agent 的 prompt 结构：
1. **翻译铁律**（见下方完整规则）——这是子 agent 必须严格遵守的约束
2. **语言特定规则**（见下方语言特定规则部分）——根据源语言选择
3. **本块原文**——完整的 Markdown 原文，含所有格式、链接、脚注

**并行执行**：所有分块的子 agent 应在同一轮中同时启动（多个 `Agent` 调用），以实现最大并行度。

#### 4C. 收集结果与校验

等待所有子 agent 完成后：
1. 检查每个子 agent 的输出，验证段落数量是否与原文一致
2. 若某块翻译明显不合格（如遗漏段落、合并句子），重新启动该块的子 agent
3. 通过验证后，按原文顺序拼接译文
4. **引用区完整性与结构校验（必须执行）**：拼接完成后，执行以下检查：

   - **结构检查**：验证 `## 注释`、`## 参考资料`、`## 外部链接` 三个标题下是否各有内容。若任一个为空，说明区段重建未完成或翻译过程中丢失了条目，必须回溯修复。
   - **内容检查**：扫描整个引用区，逐行检查是否残留未翻译的英文条目。若发现任意条目中**标题和说明文字仍为英语原文**，必须对该条目重新翻译。
   
   此项校验不可跳过。

---

## 翻译铁律 (Non-negotiable)

以下规则为子 agent 的强制约束，**绝对不可违反**。宁可翻译得生硬，也不可违反。

### 结构层面

1. **逐句对应，一句不差**：原文有多少个句子，译文就有多少个句子。禁止合并相邻句子，禁止把一个句子拆成多个。标点符号（句号、问号、感叹号）的位置一一对应。
2. **段落数量完全一致**：原文有 N 个段落，译文必须有 N 个段落。一个段落都不能合并或拆分。
3. **标题层级不变**：`#` 数量完全相同。标题文本逐字翻译。
4. **列表项一一对应**：有序列表的序号、无序列表的层级和项目数量完全一致。
5. **引用块嵌套层级不变**：`>`、`>>`、`>>>` 的层级结构完全保留。

### 内容层面

6. **不增**：禁止添加任何原文没有的信息，包括但不限于：解释性文字、背景介绍、译者注、评价、总结、过渡句。
7. **不减**：禁止删除原文包含的任何信息，包括但不限于：定语、状语、限定词、括号内容、脚注、括号内的补充说明。
8. **不改**：禁止改写、润色、优化、简化原文的表达方式。原文啰嗦就翻译得啰嗦，原文重复就翻译得重复。
9. **信息顺序不变**：句子内部的信息排列顺序与原文保持一致，不调整因果、条件、让步等逻辑关系的先后。

### 专有名词

10. **人名**：首次出现时翻译为中文 + 括号保留原文。如 `Sakamoto Yoshio` → 坂本贺勇（Sakamoto Yoshio）。后续出现可只用中文。
11. **地名**：直接翻译。`Tokyo` → 东京，`Kyoto` → 京都。
12. **作品名/游戏名**：使用公认的中文译名；无公认译名则保留原文并括号加译。`Super Metroid` → 超级密特罗德（Super Metroid）。
13. **公司/组织名**：使用官方中文名；无则保留原文。`Nintendo` → 任天堂。
14. **专业术语**：首次翻译 + 括号保留原文。`Samus Aran` → 萨姆斯·阿兰（Samus Aran）。

### 格式层面

15. **所有 Markdown 格式原样保留**：粗体 `**text**`、斜体 `*text*`、删除线 `~~text~~`、行内代码 `` `code` ``、代码块 ` ``` `、水平线 `---`。
16. **图片**：`![alt](url)` 的 alt 文本翻译为中文，URL **原封不动**。图片前后的空行保留。
17. **链接**：`[显示文本](url)` 的显示文本翻译为中文，URL **原封不动**。脚注 `[^1]` 和锚点链接完整保留。
18. **表格**：行数、列数、对齐方式不变。表头翻译，单元格内容翻译。HTML 表格转为 Markdown 表格。
19. **数字/日期/统计**：所有数值、日期格式、百分比、单位保持原文样式，不转换格式。

### 引用与来源

20. **References / 参考文献 / 出典**：文末的引用部分**逐条完整保留并逐条翻译**。每条引用的格式不变，链接不变，**标题文本和说明文字必须翻译为中文**。这是 Wiki 和访谈类文档最重要的部分，绝对不能省略或跳过任何一条。**禁止以 `tail`、`cat >>` 等文件追加方式将原始英语引用直接拼接到译文文件中**——每条引用必须经过翻译流程。

21. **区段分离（强制执行）**：Wikipedia 等页面的引用体系由多个独立区段组成——**注释 (Notes)** 使用字母标号 (a/b/c) 包含解释性文字、**参考资料 (References)** 使用数字标号包含结构化引文、**外部链接 (External links)** 使用列表项包含推荐链接。这三个区段在译文中必须各自独立、各有内容。**禁止将三类条目合并到一个平坦的 `[^n]` 列表中**。若源提取工具（如 defuddle）已将区段拍平，必须在第二步第 5 项中完成重建后再进入翻译流程。区段分类标准：解释性段落 → 注释；结构化引文（作者-日期-标题-出版物-URL）→ 参考资料；列表式外部 URL → 外部链接。

---

## 语言特定规则

### 英语 → 中文

- 被动语态按中文习惯处理（`It was developed by A` → 由 A 开发），但保留原文的信息焦点
- 英文长句中按意群自然断句…但在输出时仍保持原文的句子边界（即一个英文句号对应一个中文句号）
- 冠词（a/an/the）在中文中自然省略
- 英文引号 `"..."` 转为中文引号「」
- 缩写首次出现时展开翻译：`FPS (First-Person Shooter)` → FPS（第一人称射击游戏）

### 日语 → 中文

- **日文汉字词陷阱**：注意与中文的差异，按中文习惯翻译：
  - `検討` → 讨论（不是"检讨"）
  - `対応` → 应对/支持（不是"对应"）
  - `情報` → 信息（不是"情报"）
  - `設定` → 设置（不是"设定"）
  - `確認` → 确认（不是"确认"）
- **片假名外来语**全部翻译为中文：`インタビュー` → 采访，`タイトル` → 标题
- **和製英語**按实际含义翻译：`ファミコン` → FC/红白机（Family Computer），`テレビ` → 电视
- **敬体/简体**：`です・ます` 体在中文中用礼貌平实的语气体现；`だ・である` 体用陈述语气
- **日语省略主语**：在上下文可推断时，中文适度补充主语（但要确保补充的是原文确实隐含的）
- **语气词**：`ね`、`よ`、`なあ`、`かも` 等语气终助词在中文中用适当的语气词（吧、呢、哦、啊）体现，但不机械对应

---

## 禁止行为（红线）

禁止以下任何行为。违反任一条即为不合格输出：

- ❌ 用"本文介绍了……""该文章讲述了……""作者认为……"之类的总结句开头
- ❌ 添加任何形式的译者注、脚注说明、括号内的解释（原文已有的括号除外）
- ❌ 改写、润色、优化原文的措辞——即使原文表达生硬或重复
- ❌ 跳过任何段落、句子、注释、脚注、参考文献条目
- ❌ 改变原文的信息顺序或逻辑结构
- ❌ 合并短段落（即使两个短段落看起来可以合并）
- ❌ 拆分长段落（即使原文一个段落很长）
- ❌ 在译文末尾添加任何总结、评论、感想
- ❌ 用省略号 `……` 替代任何原文内容

---

### 第五步：组装输出文档（主 agent）

将各子 agent 的输出按原文顺序拼接后，按以下模板组装。

#### 纯中文模式（默认）

```markdown
---
source: [原始URL 或 本地文件路径]
translated: YYYY-MM-DD
original_title: "[原标题]"
tags:
  - translation
---

# 中文翻译后的标题

[纯中文正文，所有 Markdown 格式完整保留]
```

#### 双语对照模式（--bilingual）

将原文和译文逐段交替排列。每个段落前用 `> **原文**` 和 `> **译文**` 标记区分。

```markdown
---
source: [原始URL 或 本地文件路径]
translated: YYYY-MM-DD
original_title: "[原标题]"
tags:
  - translation
bilingual: true
---

# 中文翻译后的标题

> **EN** The original paragraph text. All markdown formatting preserved.

> **ZH** 翻译后的中文段落。所有 Markdown 格式保留。

![图片中文描述](image-url)

> **EN** Second paragraph.

> **ZH** 第二个段落。
```

**双语对照的具体规则：**
- 段落标签使用原文语言代码：英文用 `EN`，日文用 `JP`
- 图片、水平线等非段落元素不包裹在 blockquote 中，正常放置
- 如果原文段落内有换行（非段落分隔），保持原样
- 引用块内的双语也保持嵌套结构：`> > **EN** ...` / `> > **ZH** ...`

### 第六步：保存文件到 Obsidian Vault

1. **确定目标目录**：
   - 默认：`c:\Users\DREAM\文档\Obsidian\OneDrive\translate-webpage\`
   - 自定义：`c:\Users\DREAM\文档\Obsidian\OneDrive\<--output 参数>\`
   - 若目录不存在，使用 PowerShell `New-Item -ItemType Directory -Force` 创建

2. **确定文件名**：使用第三步生成的中文标题。若同名文件已存在，加数字后缀（`-2`、`-3`…）

3. **写入文件**：使用 `Write` 工具，编码 UTF-8

### 完成后报告

文件保存成功后，向用户报告以下信息：

```
✅ 翻译完成
📄 文件: translate-webpage/xxx.md
🌐 来源: [原始URL]
📝 语言: [英语/日语] → 中文
📏 内容: 约 X 段 / Y 句 / Z 字
🖼️ 图片: N 张（全部保留）
🔗 引用链接: M 条（全部保留）
```

---

## 多 URL 并行模式

当输入包含多个 URL（或通过 `@file` 传入 URL 列表）时，采用两级并行架构。

### 架构总览

```
主 agent (当前模型)                       子 agent (sonnet) × N
═══════════════════════                  ═══════════════════════════
                                          ┌─→ url1: fetch→clean→chunk→翻译→返回完整MD
  解析所有 URL                            │
  启动 N 个子 agent（同一轮并行）  ──→    ├─→ url2: fetch→clean→chunk→翻译→返回完整MD
  收集结果                                │
  依次写入所有文件                        ├─→ url3: fetch→clean→chunk→翻译→返回完整MD
  输出汇总报告                            │
                                          └─→ url4: ...（以此类推，最多8个并行）
```

每个 URL 由一个独立的子 agent（`model: "sonnet"`）端到端负责整条流水线。所有子 agent 在同一轮中同时启动。

### M1. 解析 URL 列表

1. 若输入为 `@file`：读取文件内容，按行解析，跳过空行和 `#` 注释行，每行一个 URL
2. 若用户通过 IDE 选区传入了文档内容：同上，按每行一个 URL 解析
3. 若为命令行空格分隔：直接拆分为 URL 列表
4. 去重：移除重复 URL，报告去重数量
5. 若 URL 数量 > 8，提示用户并仅处理前 8 个（远程子 agent 并发上限）

### M2. 启动子 Agent（同一轮全部并行）

对每个 URL，构造以下 prompt 并启动一个 `model: "sonnet"` 子 agent：

```
你是一个网页翻译子 agent。你的任务是：
1. 使用 defuddle 或 WebFetch 获取以下 URL 的网页内容
2. 清洗并提取干净的 Markdown（去除导航、侧栏、广告、页脚）
3. 根据翻译铁律逐句翻译为中文
4. 以指定的 frontmatter + Markdown 格式返回完整译文

=== 翻译铁律（必须严格遵守）===
[此处粘贴完整的翻译铁律第1-20条]

=== 语言特定规则 ===
[此处粘贴语言特定规则]

=== 输出格式 ===
返回完整的翻译后 Markdown，以 frontmatter 开头：
---
source: <URL>
translated: YYYY-MM-DD
original_title: "<原标题>"
tags:
  - translation
[若 --bilingual，添加 bilingual: true]
---

# <翻译后的中文标题>

<翻译后的正文>

=== 目标 URL ===
<URL>

=== 双语模式 ===
<true 或 false>
```

所有子 agent 的 `Agent` 调用应在同一条消息中发出（同时启动），不可逐个串行。

### M3. 收集结果

等待所有子 agent 完成后：
1. 检查每个子 agent 是否成功返回（非空、格式正确）
2. 失败的 URL 记录到汇总报告中
3. 对每个成功返回的译文，执行**引用区完整性校验**：扫描引用区是否残留未翻译的英文条目
4. 若某译文引用区校验不通过，重新启动该 URL 的子 agent（仅重试翻译引用区）
5. 通过的译文暂存，待写入

### M4. 写入文件

对每个成功翻译的 URL：
1. 从子 agent 返回的内容中提取 frontmatter 和正文
2. 根据 frontmatter 中的标题确定文件名
3. 若同名文件已存在，加数字后缀
4. 使用 `Write` 工具依次写入

### M5. 汇总报告

所有文件写入后，输出表格化汇总：

```
✅ 批量翻译完成
📁 输出目录: translate-webpage/

| # | 文件 | 来源 | 状态 |
|---|------|------|------|
| 1 | 塞尔达传说.md | en.wikipedia.org/.../Zelda | ✅ |
| 2 | 银河战士.md | en.wikipedia.org/.../Metroid | ✅ |
| 3 | xxx.md | ... | ❌ 超时 |

📊 成功 2/3
```

---

## 示例用法

```
# 单 URL
/translate-webpage https://en.wikipedia.org/wiki/Metroid
/translate-webpage https://www.nintendo.co.jp/interview/xxx/index.html --bilingual

# 多 URL（空格分隔）
/translate-webpage https://en.wikipedia.org/wiki/Zelda https://en.wikipedia.org/wiki/Mario --output games/

# 从文件读取 URL 列表
/translate-webpage @urls.txt --bilingual

# 通过 IDE 选区传入（用户在编辑器中选中包含 URL 的文档后调用）
/translate-webpage --bilingual --output lib/research/

# 本地 HTML + URL 混合
/translate-webpage "C:\Downloads\interview.html" https://example.com/article2

# Web Archive
/translate-webpage https://web.archive.org/web/20060902091638/http://www.nindori.com/interview/150/index.html
```

## 错误处理

| 情况 | 处理方式 |
|------|----------|
| URL 无法访问 | 报告 HTTP 状态码，建议检查 URL 或使用 Web Archive |
| 本地文件不存在 | 报告路径错误，要求用户确认 |
| 页面内容为空/无实际内容 | 报告并跳过，不生成空文件 |
| 页面已是中文 | 仅做格式转换（HTML→MD），跳过翻译，在报告中注明 |
| 需要登录/付费墙 | 告知用户，建议提供本地 HTML 文件 |
| 页面过大（>3万字） | 按4A策略分块，多子agent并行翻译后拼接，确保跨块引用一致 |
| 多URL超过8个 | 提示用户，仅处理前8个（子agent并发上限） |
| @file 文件不存在 | 报告路径错误，要求用户确认文件路径 |
| @file 内容无有效URL | 报告并跳过，提示文件格式要求（每行一个URL） |
| URL 列表中包含重复 | 自动去重，在汇总报告中注明显去重数量 |
